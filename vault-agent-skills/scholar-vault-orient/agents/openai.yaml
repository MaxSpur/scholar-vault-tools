interface:
  display_name: "Scholar Vault Orient"
  short_description: "Map vault papers, runs, queues, and issues"
  default_prompt: "Use $scholar-vault-orient to map the relevant papers, runs, prompt packs, queue/feedback state, topics, and open issues in this vault."
