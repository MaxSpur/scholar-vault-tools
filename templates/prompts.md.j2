# Prompt Index

{% if runs %}
{% for run in runs -%}
- [{{ run_display_title(run.title, run.prompt) }}](<../{{ run_markdown_path(run) }}>) (`{{ run.date }}`, `{{ run.result_count }}` results)
{% endfor %}
{% else %}
No Scholar Labs runs have been imported yet.
{% endif %}
